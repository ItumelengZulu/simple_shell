#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Return: Always 0 (success)
 */
int main(void)
{
    printf("This code should pass the Betty checks, with only a printf function!\n");
    return (0);
}

